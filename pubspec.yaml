name: flutter_bottom_navigation_with_nested_routing_tutorial
description: Flutter Bottom Navigation with Nested Routing Tutorial.
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  auto_route: ^5.0.2
  salomon_bottom_bar: ^3.3.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.3.2
  auto_route_generator: ^5.0.3
  flutter_lints: ^1.0.4
flutter:
  uses-material-design: true